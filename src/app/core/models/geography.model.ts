export interface Uf {
  id: number;
  nome: string;
  sigla: string;
}

export interface Cidade {
  id: number;
  nome: string;
  slug: string;
}

export interface Bairro {
  id: number;
  nome: string;
  slug: string;
}
